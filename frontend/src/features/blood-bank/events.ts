import { type Screen, type Page } from "./api";
export type EventKind = "donation" | "benefit";
export type Totals = { donations: number; benefits: number; unique_people: number };
export type Person = { id: number; facility_id: number; code: string; name: string; patient_id: number | null; patient_code: string | null; person: Record<string, string | number | null>; blood_group: string | null; rh: string | null; governorate_name: string | null; city_name: string | null; aliases: string[]; legacy_screenings: Screen[]; lock_version: number; totals: Totals; is_active: boolean };
export type BloodEvent = { id: number; person_id: number; facility_id: number; person_code: string; patient_code: string | null; name: string; kind: EventKind; benefit_kind: "issue" | "transfusion" | null; issue_event_id: number | null; linked_transfusion_id: number | null; code: string; occurred_on: string; quantity: string; quantity_unit: "kg" | "unit"; blood_group: string | null; rh: string | null; blood_component_id: number | null; component_name: string | null; clinic_id: number | null; clinic_name: string | null; responsible_staff_id: number | null; doctor_name: string | null; beneficiary_entity: string | null; entity_address: string | null; legacy_address: string | null; status: string; voided_at: string | null; legacy: boolean; lock_version: number; screenings: Screen[]; aliases: string[]; legacy_screenings: { name_ar: string; tested_on: string | null }[] };
export type EventPage = Page<BloodEvent> & { totals: Totals };
export const eventLabel = (e: Pick<BloodEvent, "kind" | "benefit_kind">) => e.kind === "donation" ? "تبرع" : e.benefit_kind === "issue" ? "استلام/صرف مكوّن دم" : "نقل دم فعلي";
export const unitLabel = (unit: string) => unit === "kg" ? "كغ" : "وحدة (تاريخية)";
