"use client";
import {useEffect,useRef,useState} from "react";
import {apiRequest,subscribeSession} from "../auth/api";
import {useClinicRequest,type Page} from "../clinics/api";
import {Pagination} from "../directory/Controls";
import {DirectoryTable} from "../directory/DirectoryPrimitives";
import OncologyEditor from "./OncologyEditor";
import {intents,modalities,sessionStates,type Dose,type Dispensing,type Plan,type Session,type EditorKind} from "./oncology";
import type {Visit} from "./api";
import {sourceLabels} from "./api";
import {dispositionLabels} from "./pathology";
import styles from "../clinics/clinics.module.css";
import oncology from "./oncology.module.css";
function excerpt(value?:string){const text=(value??"").replace(/\s+/g," ").trim();return text?text.length>90?`${text.slice(0,90)}…`:text:"دون نص";}
type MedicationPane="unlinked"|"outside";
type Props={facility:number;dossier:number;caps:Record<string,boolean>;revision:number;onChanged:()=>void;visit?:Visit;medicationView?:MedicationPane;medicationSource?:string|null};
type Editing={kind:EditorKind;plan?:Plan;session?:Session;dose?:Dose;dispensing?:Dispensing;dispensePurpose?:"unlinked"|"take_home"|"supportive"};
export default function OncologyPanel(props:Props){const {facility,dossier,caps,revision,onChanged,visit,medicationView,medicationSource}=props;const allowed=!!caps.treatment_view;
 const [page,setPage]=useState(1),[size,setSize]=useState(10),[sessionPage,setSessionPage]=useState(1),[sessionSize,setSessionSize]=useState(10),[editing,setEditing]=useState<Editing|null>(null),[details,setDetails]=useState<Plan|null>(null),[error,setError]=useState<string|null>(null),[busy,setBusy]=useState(false);
 const pending=useRef<AbortController|null>(null);
 useEffect(()=>{const clear=()=>{pending.current?.abort();setEditing(null);setDetails(null);};const unsub=subscribeSession(clear);return()=>{unsub();pending.current?.abort();};},[]);
 const base=`dossiers/${dossier}`;
 const plans=useClinicRequest<Page<Plan>&{readiness:{disposition:string}|null;next_dose:{planned_on:string}|null}>(allowed&&!visit?`${base}/treatment-plans?facility_id=${facility}&page=${page}&per_page=${size}`:null,true,true,revision);
 const sessions=useClinicRequest<Page<Session>>(allowed&&!medicationView?`${base}/treatment-sessions?facility_id=${facility}&page=${sessionPage}&per_page=${sessionSize}`:null,true,true,revision);
 const planSessions=useClinicRequest<Page<Session>>(details&&!visit?`${base}/treatment-sessions?facility_id=${facility}&plan_id=${details.id}&per_page=50`:null,true,true,revision);
 const actual=useClinicRequest<{doses:Dose[];dispensed:Dispensing[]}>(allowed&&visit?`${base}/visits/${visit.id}/doses?facility_id=${facility}`:null,false,true,revision);
 async function loadPlan(id:number,kind?:EditorKind,session?:Session){if(pending.current)return;const c=new AbortController();pending.current=c;setBusy(true);setError(null);try{const plan=await apiRequest<Plan>(`${base}/treatment-plans/${id}?facility_id=${facility}`,{signal:c.signal});if(!c.signal.aborted){if(kind)setEditing({kind,plan,session});else setDetails(plan);}}catch(e){if(!c.signal.aborted)setError(e instanceof Error?e.message:"تعذّر تحميل الخطة.");}finally{if(pending.current===c)pending.current=null;if(!c.signal.aborted)setBusy(false);}}
 if(!allowed)return null;
 const administrable=(s:Session)=>!!visit&&!!s.plan_id&&s.planned_on===visit.visit_date&&s.revision_id===s.current_revision_id&&!s.dose_id&&["scheduled","rescheduled"].includes(s.status);
 const sessionGroups=visit?[{label:"جلسات متاحة للحضور",rows:sessions.data?.data.filter(administrable)??[]},{label:"مواعيد غير متاحة للإعطاء في هذه الزيارة",rows:sessions.data?.data.filter(s=>!administrable(s))??[]}]:[{label:"كل المواعيد",rows:sessions.data?.data??[]}];
 const changed=()=>{setEditing(null);setDetails(null);onChanged();};
 const dispensed=actual.data?.dispensed??[];
 const purposeOf=(row:Dispensing)=>row.dispensing_purpose==="take_home"?"take_home":row.dispensing_purpose==="supportive"?"supportive":"unlinked";
 const paneTitle=medicationView==="unlinked"?"صرف غير مرتبط بالجرعة":medicationView==="outside"?"صرف خارج المشفى":visit?"سجل الجرعات والأدوية المصروفة للزيارة":"الخطة ومواعيد العلاج";
 const paneLabel=medicationView?"صرف أدوية الزيارة":visit?"العلاج الفعلي للزيارة":"الخطة ومواعيد العلاج";
 return <section className={styles.detailPanel} aria-label={paneLabel}><header className={styles.heading}><div><h2>{paneTitle}</h2><p>{medicationView?`الزيارة ${visit?.visit_no} · ${visit?.visit_date}. هذا الصرف لا يرتبط بجرعة. الأدوية المرتبطة بالجرعة تُفتح فقط عند تسجيل الجرعة.`:visit?`الزيارة ${visit.visit_no} · ${visit.visit_date}. أدوية الجرعة تُسجل داخل تسجيل الجرعة، والصرف المستقل لا يرتبط بها.`:"الخطة عدة مواعيد. فتح الخطة يعرض جلساتها، ولكل جلسة جرعة علاجية أو أكثر."}</p></div></header>{!visit&&<div className={oncology.choices}>{caps.treatment_create&&<div className={oncology.choice}><button className={styles.primary} onClick={()=>setEditing({kind:"plan"})}>إضافة خطة علاجية</button><small>النمط، النية، نص البروتوكول، والطبيبان. ثم تُضاف مواعيد الجلسات داخل الخطة.</small></div>}{caps.treatment_schedule&&<div className={oncology.choice}><button className={styles.secondary} onClick={()=>setEditing({kind:"appointment"})}>موعد بدون خطة</button><small>جلسة واحدة بتاريخ وطبيب، ويمكن تسجيل جرعاتها دون خطة.</small></div>}</div>}
 {error&&<p role="alert">{error}</p>}{busy&&<p role="status">جارٍ تحميل النسخة الحالية…</p>}
 {!visit&&<>{plans.loading&&<p role="status">جارٍ تحميل الخطط…</p>}{plans.error&&<p role="alert">{plans.error}<button onClick={plans.retry}>إعادة المحاولة</button></p>}{plans.data&&<><div className={styles.resultSummary}><span>الاستعداد التشخيصي: {dispositionLabels[plans.data.readiness?.disposition??"not_assessed"]}</span><strong>الموعد القادم: {plans.data.next_dose?.planned_on??"لا موعد مؤهل حاليًا"}</strong></div><DirectoryTable label="الخطط العلاجية" headers={["الخطة","البروتوكول","الإجراءات"]} busy={plans.loading}>{plans.data.data.map(plan=><tr key={plan.id}><td><bdi>{plan.plan_number}</bdi><small className={styles.hint}>{modalities[plan.modality??""]} · {intents[plan.intent??""]}</small></td><td>{excerpt(plan.protocol_text)}</td><td><div className={styles.actions}><button className={styles.primary} disabled={busy} onClick={()=>void loadPlan(plan.id)}>فتح الخطة</button>{caps.treatment_update&&<button className={styles.secondary} disabled={busy} onClick={()=>void loadPlan(plan.id,"plan")}>تعديل الخطة</button>}</div></td></tr>)}</DirectoryTable><Pagination meta={plans.data.meta} onPage={setPage} onPageSize={n=>{setSize(Number(n));setPage(1);}}/></>}
 {details&&<section className={styles.panel}><div className={styles.toolbar}><h3>جلسات {details.plan_number}</h3><div className={styles.actions}>{caps.treatment_schedule&&<button className={styles.primary} disabled={busy} onClick={()=>setEditing({kind:"schedule",plan:details})}>إضافة موعد للخطة</button>}<button className={styles.secondary} onClick={()=>setDetails(null)}>إغلاق</button></div></div>{details.revisions[0]&&<p>{excerpt(String(details.revisions[0].protocol_text??""))} · {details.revisions[0].treating_doctor_name}</p>}{planSessions.loading&&<p role="status">جارٍ تحميل جلسات الخطة…</p>}{planSessions.data&&<DirectoryTable label={`جلسات ${details.plan_number}`} headers={["الجلسة","تاريخ الموعد","الجرعات العلاجية","الإجراءات"]}>{planSessions.data.data.map(s=><tr key={s.id}><td>جلسة {s.session_number}</td><td>{s.planned_on}</td><td>{(s.doses??[]).length?s.doses!.map(dose=><small key={dose.id} className={styles.hint}>{dose.dose_name} · {dose.given_on} · {sourceLabels[dose.medication_source??""]??""} · {dose.nurse_name}</small>):<small className={styles.hint}>لا جرعات بعد</small>}</td><td><div className={styles.actions}>{caps.treatment_schedule&&["scheduled","rescheduled"].includes(s.status)&&<button className={styles.secondary} onClick={()=>setEditing({kind:"session-dose",plan:details,session:s})}>إضافة جرعة علاجية</button>}</div></td></tr>)}{!planSessions.data.data.length&&<tr><td colSpan={4}>لا مواعيد بعد. أضف موعد الجلسة الأولى.</td></tr>}</DirectoryTable>}</section>}</>}
 {!medicationView&&<><h3>{visit?"اختيار جلسة لتسجيل حضورها وإعطائها فعلًا":"المواعيد"}</h3>{sessions.loading&&<p role="status">جارٍ تحميل المواعيد…</p>}{sessions.error&&<p role="alert">{sessions.error}<button onClick={sessions.retry}>إعادة المحاولة</button></p>}{sessions.data&&<>{sessionGroups.map(group=><section key={group.label}>{visit&&<h4>{group.label}</h4>}<DirectoryTable label={group.label} headers={["الخطة / الجلسة","التاريخ المخطط","حالة الموعد","الإجراءات"]} busy={sessions.loading}>{group.rows.map(s=><tr key={s.id}><td>{s.plan_id?`${s.plan_number} / ${s.session_number}`:"موعد مستقل"}</td><td>{s.planned_on}</td><td>{sessionStates[s.status]}{!s.plan_id&&<small className={styles.hint}>موعد دون خطة.</small>}{(s.doses??[]).map(dose=><small key={dose.id} className={styles.hint}>{dose.dose_name} · {dose.given_on}{dose.medication_source?` · ${sourceLabels[dose.medication_source]??""}`:""}</small>)}{s.revision_id!==s.current_revision_id&&s.status!=="completed"&&<small className={styles.hint}>نسخة علاجية سابقة — تحتاج معالجة قبل الإعطاء</small>}{visit&&s.planned_on!==visit.visit_date&&<small className={styles.hint}>أعد الجدولة صراحة إلى تاريخ الحضور قبل الإعطاء.</small>}{s.has_voided_dose&&<small>توجد وقائع إعطاء مبطلة محفوظة تاريخيًا</small>}</td><td><div className={styles.actions}>{!visit&&caps.treatment_schedule&&["scheduled","rescheduled"].includes(s.status)&&<button className={styles.secondary} onClick={()=>setEditing({kind:"session-dose",session:s})}>إضافة جرعة علاجية</button>}{caps.treatment_schedule&&s.status!=="completed"&&<button className={styles.secondary} onClick={()=>setEditing({kind:"session",session:s})}>تغيير الموعد أو حالته</button>}{visit&&s.plan_id&&s.planned_on===visit.visit_date&&s.revision_id===s.current_revision_id&&!s.dose_id&&caps.treatment_administer&&["scheduled","rescheduled"].includes(s.status)&&<button className={styles.primary} disabled={busy||sessions.loading} onClick={()=>void loadPlan(s.plan_id!,"dose",s)}>تسجيل إعطاء جرعة</button>}{s.visit_id&&<span>موثقة في الزيارة #{s.visit_id}</span>}</div></td></tr>)}{!group.rows.length&&<tr><td colSpan={4}>لا توجد جلسات ضمن هذه الصفحة.</td></tr>}</DirectoryTable></section>)}<Pagination meta={sessions.data.meta} onPage={setSessionPage} onPageSize={n=>{setSessionSize(Number(n));setSessionPage(1);}}/></>}</>}
 {visit&&<>{actual.loading&&<p role="status">جارٍ تحديث الوقائع الفعلية…</p>}{actual.error&&<p role="alert">{actual.error}<button onClick={actual.retry}>إعادة المحاولة</button></p>}{actual.data&&<>{!medicationView&&<><h3>الإعطاء الفعلي</h3>{actual.data.doses.map(d=>{const linked=dispensed.filter(row=>row.dose_session_id===d.id);return <section className={styles.panel} key={d.id}><div className={styles.toolbar}><h4>جلسة #{d.id} · {d.administered_on} {d.voided_at&&"· إعطاء مبطل — محفوظ تاريخيًا"}</h4><div className={styles.actions}>{!d.voided_at&&d.oncology_session_id&&<>{caps.treatment_correct&&<button className={styles.secondary} onClick={()=>setEditing({kind:"dose",dose:d})}>تصحيح الإعطاء</button>}{caps.treatment_void&&caps.treatment_schedule&&<button className={`${styles.secondary} ${styles.dangerText}`} onClick={()=>setEditing({kind:"void-dose",dose:d})}>إبطال الإعطاء</button>}{caps.treatment_dispense&&<button className={styles.primary} onClick={()=>setEditing({kind:"dispense",dose:d,dispensePurpose:"supportive"})}>دواء مرتبط بهذه الجرعة</button>}</>}</div></div><p>{d.note} {d.void_reason}</p><DirectoryTable label={`أدوية مرتبطة بالجرعة ${d.id}`} headers={["اسم الدواء المحفوظ","الجرعة","الكمية","الحالة"]}>{d.items.map(i=><tr key={i.id}><td>{i.medication_name_snapshot}</td><td>{i.dose_value} {i.dose_unit}</td><td>{i.quantity} {i.quantity_unit}</td><td>{d.voided_at?"إعطاء مبطل — محفوظ تاريخيًا":i.voided_at?"بند مبطل مع حفظ تاريخه":"إعطاء مسجل"}{i.void_reason&&<small>{i.void_reason}</small>}</td></tr>)}</DirectoryTable>{!!linked.length&&<DirectoryTable label={`صرف مرتبط بالجرعة ${d.id}`} headers={["اسم الدواء المحفوظ","الكمية","الحالة","الإجراءات"]}>{linked.map(row=><tr key={row.id}><td>{row.medication_name_snapshot}{row.voided_at&&" · مبطل"}{row.parent_voided_at&&!row.voided_at&&<small className={styles.hint}>صرف فعال مستقل مرتبط بإعطاء مبطل</small>}</td><td>{row.quantity} {row.quantity_unit}</td><td>دواء مرتبط بالجرعة</td><td>{!row.voided_at&&<div className={styles.actions}>{caps.treatment_correct&&<button className={styles.secondary} onClick={()=>setEditing({kind:"dispense",dispensing:row,dispensePurpose:"supportive"})}>تصحيح الصرف</button>}{caps.treatment_void&&<button className={`${styles.secondary} ${styles.dangerText}`} onClick={()=>setEditing({kind:"void-dispense",dispensing:row})}>إبطال الصرف</button>}</div>}</td></tr>)}</DirectoryTable>}</section>;})}</>}{medicationView&&<>{caps.treatment_dispense&&<div className={styles.actions}><button type="button" className={styles.primary} onClick={()=>setEditing({kind:"dispense",dispensePurpose:medicationView==="outside"?"take_home":"unlinked"})}>{medicationView==="outside"?"تسجيل صرف خارج المشفى":"تسجيل صرف غير مرتبط بالجرعة"}</button></div>}<p className={styles.hint}>الأدوية المرتبطة بالجرعة تُفتح فقط عند تسجيل الجرعة.</p><DirectoryTable label={paneTitle} headers={["اسم الدواء المحفوظ","تاريخ الصرف","الكمية","الإجراءات"]}>{dispensed.filter(d=>d.dispensing_purpose===(medicationView==="outside"?"take_home":"unlinked")).map(d=><tr key={d.id}><td>{d.medication_name_snapshot}{d.voided_at&&" · مبطل"}</td><td>{String(d.dispensed_on??"")}</td><td>{d.quantity} {d.quantity_unit}</td><td>{!d.voided_at&&<div className={styles.actions}>{caps.treatment_correct&&<button className={styles.secondary} onClick={()=>setEditing({kind:"dispense",dispensing:d,dispensePurpose:purposeOf(d)})}>تصحيح الصرف</button>}{caps.treatment_void&&<button className={`${styles.secondary} ${styles.dangerText}`} onClick={()=>setEditing({kind:"void-dispense",dispensing:d})}>إبطال الصرف</button>}</div>}</td></tr>)}</DirectoryTable></>}</>}</>}
 {editing&&<OncologyEditor {...editing} facility={facility} dossier={dossier} visit={visit} caps={caps} medicationSource={medicationSource} onClose={()=>setEditing(null)} onSaved={changed} onRefresh={onChanged}/>}</section>;
}
